export type ServiceType = {
  id: number;
  icon: JSX.Element;
  title: string;
  paragraph: string;
};

export type AboutUsType = {
  id: number;
  icon: JSX.Element;
  title: string;
  paragraph: string;
};